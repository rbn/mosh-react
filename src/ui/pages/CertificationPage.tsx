import React from "react";
import { Outlet } from "react-router-dom";

const CertificationPage = () => {
  return (
    <main>
      <section>
        <h1 className="flex">Cert</h1>
      </section>

      <section></section>
    </main>
  );
};

export default CertificationPage;
